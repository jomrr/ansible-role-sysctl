# {{ template_destpath }}
# vim: set noai sw=4 tw=4 ts=4 et:
# {{ ansible_managed }}
# entries: {{ item.settings | length }}

{% for setting in item.settings | dict2items | sort(attribute='key') %}
{{ setting.key | string }}={{ setting.value }}
{% endfor %}