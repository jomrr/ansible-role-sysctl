# {{ template_destpath }}
# vim: set noai sw=4 tw=4 ts=4 et:
# {{ ansible_managed }}
# entries: {{ item.settings | length }}

{% for setting in item.settings | sort(attribute='key') %}
{%  if setting.comment is defined %}
# {{ setting.comment | string }}
{%  endif %}
{{ setting.key | string }}={{ setting.value }}
{% endfor %}